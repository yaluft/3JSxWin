# markdownlint configuration
# https://github.com/DavidAnson/markdownlint/blob/main/doc/Rules.md

default: true

# Allow duplicate headings in different sections
MD024:
  siblings_only: true

# Do not treat YAML front-matter title as a top-level heading
MD025:
  front_matter_title: ""

# Line length — disable for table-heavy docs
MD013: false

# Allow raw HTML (used for video embed in README)
MD033: false

# Allow inline HTML
MD041: false
